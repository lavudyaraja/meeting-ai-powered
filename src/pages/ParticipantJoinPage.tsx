import { ParticipantJoin } from "@/components/meetings";

const ParticipantJoinPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-50 via-purple-50 to-pink-50">
      <ParticipantJoin />
    </div>
  );
};

export default ParticipantJoinPage;